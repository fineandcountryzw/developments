
import React from 'react';
import { Role } from '../types.ts';

interface MobileFABProps {
  role: Role;
}

export const MobileFAB: React.FC<MobileFABProps> = ({ role }) => {
  // Component disabled - Site Progress Photo and Field Note Entry buttons removed
  return null;
};
